# Kubernetes Troubleshooting Copilot Requirements

# --- Core Web Framework (Cloud Run) ---
fastapi>=0.111.0               # The core asynchronous REST framework
uvicorn[standard]>=0.30.0      # ASGI server to run FastAPI
httpx>=0.27.0                  # Required for async requests and FastAPI TestClient

# --- Schema Validation ---
pydantic>=2.7.0                # Enforces strict JSON schemas for ExecutorAgent output

# --- Google Cloud Platform Core ---
google-auth>=2.30.0            # Handles SPIFFE-based identities in the Argolis Sandbox
google-cloud-secret-manager    # Secures API keys and credentials
google-cloud-logging           # Asynchronous structured JSON logging to Cloud Logging
google-cloud-bigquery          # Telemetry sink for token consumption / cost tracking
google-cloud-firestore         # Storing SRE session metrics and thumbs up/down feedback

# --- Google GenAI & Search ---
google-adk[a2a]>=2.6.2         # Agent Development Kit framework
google-genai>=1.0.0            # Google GenAI SDK (Gemini 2.5 Pro/Flash)
google-cloud-aiplatform        # Vertex AI SDK (Access to Gemini 2.5 Pro and Flash)
google-cloud-discoveryengine   # Vertex AI Search SDK (Querying the K8s grounded index)

# --- Observability (OpenTelemetry) ---
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-fastapi  # Auto-instruments the FastAPI endpoints
opentelemetry-exporter-gcp-trace       # Exports spans to Google Cloud Trace

# --- Data Ingestion & Tooling ---
pyyaml>=6.0.1                  # Safe YAML loading for externalized agent system prompts
python-frontmatter             # Parses and strips Hugo Markdown frontmatter for the GCS ingestion pipeline
mcp>=1.2.0,<2.0.0              # Anthropic Model Context Protocol SDK with FastMCP support

# --- Testing & Evaluation Framework ---
pytest>=8.2.0                  # Test runner
pytest-asyncio                 # Required for testing async FastAPI endpoints
pytest-cov                     # Enforces the >80% code coverage requirement from the Scope doc
